RewriteEngine On
RewriteBase /atasko/

# No reescribir si el recurso existe
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirigir todo lo demás a index.php
RewriteRule ^ index.php [L,QSA]